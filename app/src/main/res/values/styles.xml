<resources>

    <!-- Base application theme. -->
    <style name="Theme.MyApp" parent="Theme.GuidelinesCompat.Light.ColoredToolbar.NoActionbar">
        <!-- Original AppCompat attributes. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowBackground">@color/primary_dark</item>
    </style>

    <style name="Theme.MyApp.Settings" parent="Theme.GuidelinesCompat.Light.ColoredToolbar">
        <!-- Original AppCompat attributes. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="Theme.MyApp.Toolbar" parent="Theme.GuidelinesCompat.ColoredToolbar">
    </style>

    <!--<item name="buttonStyle">@style/Widget.AppCompat.Button.Colored</item>-->

</resources>
